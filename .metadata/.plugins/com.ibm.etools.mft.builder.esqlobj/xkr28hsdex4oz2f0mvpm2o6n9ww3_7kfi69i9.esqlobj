/*EATE FUNCTION Main() RETURNS BOOLE*/
	BEGIN
		
		IF SESSION.Field IS NOT NULL THEN
			DECLARE I, J INTEGER;
			SET I = 1;
			SET J = CARDINALITY(Session.*[]);
			
			WHILE I <= J DO
				
				SET I = I + 1;
			END WHILE;
			
		END IF;
		
		RETURN TRUE;
	END;
